# Owner Checkpoint Report

Using the template in `05_CLAUDE_CODE_EXECUTION/OWNER_CHECKPOINT_REPORT_TEMPLATE.md`.

1. **Checkpoint/page:** Checkpoint 5 — Purchase Order (P07) and Fulfilment / GRN & Service Entry (P08).
2. **Branch and commit:** `claude/design-authority-review-azeq82`, on top of Checkpoints 1–4 (+ Checkpoint 1 addendum).
3. **Production files changed:** `src/server/purchase-order.ts` (new), `src/server/fulfilment.ts` (new), `src/server/actions/purchase-order-actions.ts` (new — Issue PO & Open Fulfilment), `src/server/actions/fulfilment-actions.ts` (new — Post Accepted Receipt), `src/app/app/projects/[projectId]/purchase-orders/[purchaseOrderId]/page.tsx` (new route), `src/app/app/projects/[projectId]/fulfilment/[fulfilmentId]/page.tsx` (new route), `src/components/procurement/OpenFulfilmentButton.tsx` (new), `src/components/procurement/PostReceiptForm.tsx` (new), `src/db/schema.ts` (new `purchase_order_lines` table; `fulfilment_entries` gets required `purchase_order_line_id` FK + `status`), `src/db/seed.ts` (golden PO's 3 real lines backfilled, golden GRN linked to its line), `src/server/actions/finance-actions.ts` (`validateAndLockRoute`'s CREDIT branch now also creates `purchase_order_lines` from the award's lines), `src/app/.../finance-validations/[validationId]/page.tsx` (PO badge is now a real link, not static text), `src/server/control-room.ts` (bug fix — see §16).
4. **Schema/migrations:** squashed fresh `drizzle/0000_bumpy_triathlon.sql` (still dev-only data, no production data to preserve). Added `purchase_order_lines` table (line-level qty/rate/net/tax/gross/currency, FK to `award_lines`); added `purchase_order_line_id` (required) and `status` (`DRAFT`/`POSTED`) to `fulfilment_entries`.
5. **Routes/services/APIs:** `/app/projects/:projectId/purchase-orders/:purchaseOrderId` and `/app/projects/:projectId/fulfilment/:fulfilmentId` — both match the pack's canonical routes. Two new Server Actions: `openFulfilment` (creates a `DRAFT` fulfilment entry for the next PO line without one, idempotent per line), `postAcceptedReceipt` (posts accepted/rejected quantities, immutable once `POSTED`).
6. **Permissions/SoD:** Unchanged disclosed gap — both new pages' own readiness sidecars honestly mark "Segregation of duties"/"SoD" as failing or deferred rather than assumed satisfied.
7. **Formula reconciliations:** see `evidence/v7/checkpoint-5/formula-reconciliation.md`. Golden GRN's certifiable value reconciles exactly to PAGE_08's own fixture (38 x GHS 4,604 = GHS 174,952). Golden PO's per-line gross/tax breakdown (only partially given in PAGE_07's fixture — concrete line only) was inferred at a uniform 20% tax rate for the other two lines and independently cross-checked against the PO's own overall gross total (already seeded since Checkpoint 1): 1,325,952 + 104,400 + 208,800 = 1,639,152 ✓, confirming the inference is consistent with the pack's numbers rather than invented. The demo chain's "Issue PO & Open Fulfilment" then "Post Accepted Receipt" transitions were both exercised live end to end on `PO-DEMO-0001` (USD), producing `GRN-DEMO-0001-L1` posted at accepted 500 / rejected 0 / certifiable US$9,595.00 (500 x 19.19), matching the seeded award line's real rate.
8. **Evidence/lineage/audit:** No audit-event log yet (same disclosed gap). Fulfilment entries link back via `purchaseOrderLineId`; PO lines link back via `awardLineId`, giving real (if partial) forward lineage from award line through PO line to fulfilment.
9. **Tests:** No automated test suite. Verification: `tsc --noEmit`, `eslint`, `next build` clean; Playwright pass across golden PO/GRN pages and the live demo Issue-Fulfilment + Post-Receipt transitions, zero console errors; re-checked Page 01 Control Room, the demo request dossier (P03), and the requests register (P02) afterward for regressions — which is exactly what caught the `activeGate` bug in §16 before it shipped.
10. **Build and deterministic install:** `npm run build` succeeds. No new dependencies.
11. **Browser states and viewports:** "Populated" only, 1440x900, plus one live before/after transition per page. Most of each contract's required-states list not covered (see `evidence/v7/checkpoint-5/screenshot-manifest.json`).
12. **Screenshot manifest and hashes:** `evidence/v7/checkpoint-5/screenshot-manifest.json`.
13. **Region-by-region authority comparison** against the pack's reference screenshots for P07/P08: not performed (same disclosed Gate 3/5 cut as every prior checkpoint). Built: P07 — header/actions (Preview PDF/Amend-Cancel disabled, Issue-Fulfilment action real), 5-cell KPI strip including a per-unit-group "fulfilled/certified balance" (deliberately not blended across m3/shift/lot — see defect note), 4 of 9 tabs real (Order Summary, Lines, Commercial Terms static, Fulfilment tab itself is "soon" — the transition lives on P08 instead), readiness sidecar (6 checks, 2 disclosed deferred). P08 — header/actions (Save Draft disabled, Post Accepted Receipt real for DRAFT state, read-only summary for POSTED), 6-cell KPI strip, receive-and-inspect form, readiness sidecar (6 checks, 2 disclosed deferred). Not attempted: quality evidence/test-reading capture, stock/allocation event, discrepancy reason capture on rejection, multiple/partial GRNs per line, reversal of a posted receipt, Full Lineage drawer, all tabs marked "soon".
14. **Console/network:** Zero console errors/warnings across all pages and both live transitions.
15. **Accessibility/performance/security:** Same disclosed gap as prior checkpoints — no automated scan.
16. **Defects:**
    - *Major (disclosed, unresolved, inherited):* no auth/RBAC; no evidence/audit engine; register row-opening behavior.
    - *Major (found and fixed this checkpoint, not left unresolved):* `control-room.ts`'s per-control-account `activeGate` label (Commercial Lifecycle Control Sheet's "Active gate/next control" column) had a two-state ternary predating Checkpoint 5 that could never read "Fulfilment / PV & Settle" unless a Payment Voucher already existed — so a control account with a fully-fulfilled-but-not-yet-certified PO (exactly what this checkpoint's live demo transition produces) incorrectly stayed labelled "Order & Commit", inconsistent with the same page's own higher-fidelity lifecycle spine one section above, which already classified it correctly. Fixed by tracking a `hasFulfilment` flag per account; re-verified against both golden and demo rows with no regression.
    - *Avoided proactively (not a recurrence):* the currency-mislabeling bug class that hit `award_decisions`, `finance_validations`, and `purchase_orders` in Checkpoints 3–4 was flagged in the Checkpoint 4 report as a risk for `fulfilment_entries`/`payment_vouchers` next. `fulfilment_entries` deliberately carries no currency column of its own — `certifiableValue` is computed by joining to `purchase_order_lines.currency` (real per-line data), so there was nothing to mislabel. Confirmed live on the USD demo chain (`P08/grn-demo-posted-1440x900.png` correctly shows US$, not GHS).
    - *Minor:* "Fulfilled/certified balance" KPI is intentionally not blended across units (m3/shift/lot shown separately) since the golden PO's three lines have incompatible units — a single blended percentage would be meaningless; per-line fulfilment is correct as implemented but does mean the golden PO's headline KPI never reaches "100%" as a single number (only per-line).
17. **Approved variations:** none sought or granted.
18. **Owner decisions required before Checkpoint 6:**
    - Confirm the inferred 20% uniform tax rate on the golden PO's pump/testing lines (only the concrete line's gross is literally stated in PAGE_07's fixture) is an acceptable inference, or should be sourced/corrected directly.
    - Checkpoint 6 (Payment Voucher, P09) will need the demo chain's now-posted GRN as its input — confirm proceeding to build the PV transition against `GRN-DEMO-0001-L1`'s accepted value (US$9,595.00) is the intended next step.
    - Multiple/partial GRNs per PO line, and a reversal/version flow for a posted receipt, remain unbuilt (single-shot immutable receipt only) — worth deciding whether Checkpoint 6+ should invest in this or keep deferring.
19. **Next proposed checkpoint (not started):** Checkpoint 6 — Page 09 (Payment Voucher) and end-to-end golden-transaction closure, per the pack's page-implementation order. Not started; no code written toward it.
20. **Status:** `NOT OWNER-ACCEPTED`.

---

**Reproduction:** same as prior checkpoints. Fresh seed leaves
`FV-DEMO-0001` already `VALIDATED`/CREDIT-locked with `PO-DEMO-0001`
issued (carried from Checkpoint 4) — open `PO-DEMO-0001`, click "Issue PO
& Open Fulfilment", then on the resulting `DRAFT` fulfilment page click
"Post Accepted Receipt" (defaults to full acceptance) to reproduce the
"after" screenshots and the live PO/GRN state changes.

CHECKPOINT 5 IMPLEMENTED AND VERIFIED — NOT OWNER-ACCEPTED — AWAITING
OWNER REVIEW — DO NOT START ANOTHER PAGE.
