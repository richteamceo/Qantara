# Owner Checkpoint Report

Using the template in `05_CLAUDE_CODE_EXECUTION/OWNER_CHECKPOINT_REPORT_TEMPLATE.md`.

1. **Checkpoint/page:** Checkpoint 7 — Phase-1 hardening (`05_CLAUDE_CODE_EXECUTION/CONTROLLED_BUILD_AND_CHECKPOINT_PROGRAMME.md`: "Full permissions/SoD, responsive/accessibility, security, performance, migration, integration, backup/restore, observability, professional outputs, regression and owner acceptance pack"). This is a cross-cutting hardening checkpoint, not a new page — scoped to a real, verifiable subset rather than attempting all eleven listed areas at once; scope and deferrals below.
2. **Branch and commit:** `claude/design-authority-review-azeq82`, on top of Checkpoints 1–6.
3. **Production files changed:** `src/lib/roles.ts` (new — client-safe Role type/labels/requireRole), `src/lib/auth.ts` (new — server-only `getActorRole` cookie read), `src/server/actions/actor-actions.ts` (new — `setActorRole`), `src/components/shell/RoleSwitcher.tsx` (new), `src/components/shell/ForbiddenNotice.tsx` (new — shared render for the `forbidden` result), `src/components/shell/TopBar.tsx`/`AppShell.tsx`/`src/app/.../layout.tsx` (thread `actorRole` through), all 8 mutating server actions (`request-actions.ts`, `procurement-actions.ts` x2, `finance-actions.ts`, `purchase-order-actions.ts`, `fulfilment-actions.ts`, `payment-voucher-actions.ts` x2) gated with a real role check, all 8 corresponding button/form client components updated to render the `forbidden` result, 3 server aggregation files (`finance-validation.ts`, `purchase-order.ts`, `payment-voucher.ts`) with their "SoD/Authority" readiness item now live instead of hardcoded `fail`, 14 inline source links across 7 page files given `underline` (accessibility fix, see §16), `OWNER_ACCEPTANCE_PACK.md` (new).
4. **Schema/migrations:** none. RBAC is cookie-based, not a schema feature (disclosed simplification, see §6).
5. **Routes/services/APIs:** no new routes. One new Server Action (`setActorRole`) and a `requireRole` gate function called at the top of all 8 existing mutating actions, before any DB access.
6. **Permissions/SoD:** this is the checkpoint's core deliverable. Implemented a real, server-enforced RBAC gate sourced directly from `01_PRODUCT_AND_PROCESS_AUTHORITY/ROLE_PERMISSION_AND_SEGREGATION_OF_DUTIES_MATRIX.md`'s "Minimum decision matrix" and "Segregation rules": 8 roles (`REQUESTER_SITE_ENGINEER`, `SITE_QS_COMMERCIAL`, `PROCUREMENT`, `FINANCE`, `PROJECT_DIRECTOR`, `RECEIVER`, `AP_TREASURY`, `AUDITOR`), each of the app's 8 mutating transitions gated to the specific role the matrix assigns it (e.g. `validateAndLockRoute` requires `FINANCE` per the matrix's explicit rule "Finance route lock requires Finance permission and cannot be granted by Procurement UI state"; `approveAndReleasePayment` requires `FINANCE`, separable from `openPaymentVoucher`'s `SITE_QS_COMMERCIAL` certification step, per "receiver cannot alone certify payment value" / "payment preparation, approval and execution are separable functions"). Every gate is checked server-side, before any database access — client-side hiding is not the enforcement mechanism (matrix §4). **Disclosed limitation, stated plainly:** there is no login/credential system. "Acting as" (top-bar dropdown) is a self-service switcher writing a plain cookie the browser itself controls. This demonstrates the *shape* of the enforcement (every action really is blocked server-side when the actor lacks the role) but is **not real security** — anyone with browser access can switch their own role. Real authentication is listed as an owner decision for the next checkpoint (`OWNER_ACCEPTANCE_PACK.md` §7.5).
7. **Formula reconciliations:** none new this checkpoint — no money formulas changed. Existing formulas re-verified as part of the regression pass (§9, §11).
8. **Evidence/lineage/audit:** no audit-event log (same disclosed gap, unchanged). The RBAC gate itself has no audit trail of who-acted-as-whom-when — a real login system would need to add this.
9. **Tests:** No automated test suite (same disclosed gap). Verification: `tsc --noEmit`, `eslint`, `next build` clean. A full regression pass re-screenshotted all 9 golden pages plus Control Room from a fresh reseed — zero console errors, no visual regressions vs. Checkpoints 1–6. A live blocked-then-allowed demonstration was run for 5 of the 8 gated transitions (the full P06→P09 chain), switching the actor role between attempts and confirming both the deny and the allow paths work end to end with no side effects from a denied attempt.
10. **Build and deterministic install:** `npm run build` succeeds. No new dependencies for the RBAC feature; `axe-core` (already a transitive devDependency) was used read-only for the accessibility scan via Playwright, not added to the app bundle.
11. **Browser states and viewports:** 1440x900. Regression: `evidence/v7/checkpoint-7/regression/` (10 screenshots — all 9 pages + Control Room, golden data). RBAC: `evidence/v7/checkpoint-7/rbac/` (10 screenshots — 5 gated actions, blocked then allowed, real server responses). Accessibility: `evidence/v7/checkpoint-7/a11y-keyboard-focus-check-1440x900.png` (25-tab keyboard walkthrough showing a real focus-visible ring).
12. **Screenshot manifest and hashes:** `evidence/v7/checkpoint-7/screenshot-manifest.json`.
13. **Region-by-region authority comparison:** not performed (same disclosed Gate 3/5 cut as every prior checkpoint) for the underlying pages — this checkpoint didn't touch page layout/content beyond the two additive changes below.
14. **Console/network:** Zero console errors/warnings across the full regression pass and all 10 RBAC transitions.
15. **Accessibility/performance/security:** First real pass this build. Ran an axe-core (WCAG 2 A/AA ruleset) scan via Playwright across 6 representative pages (Control Room, Requests Register, Request Dossier, Finance Validation, Purchase Order, Payment Voucher). Found and fixed for real: a `link-in-text-block` violation on 2+ inline "Source:" links (distinguished from surrounding text by color alone, violating the pack's own "colour never carries state alone" rule in `ACCESSIBILITY_PERFORMANCE_AND_POLISH_GATES.md`) — fixed by adding `underline` to all 14 occurrences of this pattern across every page in the app, re-scanned, confirmed zero `link-in-text-block` violations afterward. Verified (not a defect): keyboard focus-visible styling (`.c1x-focusable`) is applied consistently across every interactive button/link/input in the codebase — confirmed live via a 25-tab keyboard walkthrough. **Found and deliberately NOT fixed — recorded as an authority conflict per `AUTHORITY_ORDER_AND_SUPERSESSION.md` §4 ("must not resolve ... visual conflicts by assumption"):** the approved v7 design tokens (`06_MACHINE_READABLE/design-tokens-v7.json`, copied verbatim into `globals.css`) fail WCAG 2 AA text-contrast (4.5:1) in several combinations used throughout every page — `--c1x-muted-2` gray-on-white (3.1:1), `--c1x-blue` on `--c1x-blue-soft` badges (4.44:1), `--c1x-amber` on white (3.06:1) — 28 to 70 flagged nodes per page, systemic, not page-specific. Source A: `EXACT_VISUAL_IMPLEMENTATION_CONTRACT`/the approved design tokens (pixel-exact fidelity). Source B: `ACCESSIBILITY_PERFORMANCE_AND_POLISH_GATES.md` ("WCAG 2.2 AA target"). Both are same-tier authority (`02_DESIGN_SYSTEM_AUTHORITY`), so the hierarchy doesn't resolve it. Risk: real, ships to every page, affects users with low vision. Recommended interpretation: this looks like an oversight in the source tokens rather than an intentional accessibility trade-off, but changing the tokens is a visual-authority change this build must not make unilaterally. **Owner decision required** — see `OWNER_ACCEPTANCE_PACK.md` §7.3. Performance/security: no automated scan (Lighthouse/SAST/dependency-audit) run — disclosed gap, listed as deferred below.
16. **Defects:**
    - *Major (disclosed, unresolved, inherited):* no real authentication (see §6); no evidence/audit engine; no forecast/cashflow model.
    - *Major (found this checkpoint, disclosed rather than silently resolved):* the design-token WCAG AA contrast conflict (§15) — a genuine authority contradiction, not a coding defect, recorded per the pack's own conflict procedure rather than picked one way or the other by assumption.
    - *Fixed this checkpoint:* `link-in-text-block` accessibility violation (§15).
    - *Minor:* RBAC blocked-then-allowed evidence covers 5 of 8 gated actions (the full downstream P06→P09 chain); the other 3 (`approvePrepareRequestPackage`, `openAwardDecision`, `approveSendToFinance`) are equally gated in code (verified by reading the source, same `requireRole` pattern) but weren't separately screenshotted since the records they'd act on are already in a terminal/idempotent state after reseed.
17. **Approved variations:** none sought or granted.
18. **Owner decisions required before Checkpoint 8:** consolidated in `OWNER_ACCEPTANCE_PACK.md` §7 — (1) the inferred PO tax rate from Checkpoint 5, (2) invest in multi-GRN/reversal flows or keep deferring, (3) **the design-token WCAG AA conflict found this checkpoint**, (4) bake the demo chain into the seed baseline or keep re-driving it live each checkpoint, (5) prioritize real authentication next or continue page/contract depth.
19. **Next proposed checkpoint (not started):** not specified by the pack beyond "Phase-1 hardening" — the pack's programme document ends at Checkpoint 7. Continuing further work requires an owner decision on direction (§18.5). Not started; no code written toward it.
20. **Status:** `NOT OWNER-ACCEPTED`.

---

**Reproduction:** same as prior checkpoints, plus role-switching. Fresh
seed leaves `FV-DEMO-0001` at `PENDING`. Before attempting any mutating
action, use the "Acting as" dropdown in the top bar to select the role
the SoD matrix assigns it (Finance for route-lock/payment-release,
Procurement for PO/fulfilment-issuance, Receiver for posting receipts,
QS/Commercial for package-prep/voucher-certification, Project Director
for award-approval) — attempting with the wrong role reproduces the
`forbidden` screenshots in `evidence/v7/checkpoint-7/rbac/`.

CHECKPOINT 7 IMPLEMENTED AND VERIFIED — NOT OWNER-ACCEPTED — AWAITING
OWNER REVIEW — DO NOT START ANOTHER CHECKPOINT.
