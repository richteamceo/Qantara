# Owner Checkpoint Report

Using the template in `05_CLAUDE_CODE_EXECUTION/OWNER_CHECKPOINT_REPORT_TEMPLATE.md`.

1. **Checkpoint/page:** Checkpoint 8 — **self-scoped**, not pack-mandated. `05_CLAUDE_CODE_EXECUTION/CONTROLLED_BUILD_AND_CHECKPOINT_PROGRAMME.md` (the pack's own checkpoint programme) ends at Checkpoint 7; `CHECKPOINT_7_REPORT.md` §18/§19 flagged this explicitly and listed direction-of-next-work as an open owner decision (`OWNER_ACCEPTANCE_PACK.md` §7.5) that has not been answered. Rather than block, this checkpoint picks one concrete, bounded item from Checkpoint 7's own deferred-items list ("professional outputs") and implements it for real, per `DOCUMENT_AND_REPORT_GENERATION_STANDARD.md`: real server-generated PDF documents for Purchase Order (P07) and Payment Voucher (P09) — the two document types this pack's standard names explicitly and that already had a disabled "Preview...PDF" placeholder or equivalent gap in the UI. The standard's much larger required-document-type list (RFQs, GRNs, board packs, quotation comparisons, etc.) is explicitly out of scope here — see §19.
2. **Branch and commit:** `claude/design-authority-review-azeq82`, on top of Checkpoints 1–7.
3. **Production files changed:** `src/server/documents/pdf.ts` (new — shared PDF layout primitives per the standard's "shared service, no stage builds its own PDF path" rule), `src/server/documents/purchase-order-pdf.ts` (new), `src/server/documents/payment-voucher-pdf.ts` (new), `src/app/.../purchase-orders/[purchaseOrderId]/pdf/route.ts` (new Route Handler), `src/app/.../payment-vouchers/[voucherId]/pdf/route.ts` (new Route Handler), `src/server/audit.ts` (new), `src/db/schema.ts` (new `audit_events` table), the two page files (enabled "Preview Order PDF"; added "Export PV PDF").
4. **Schema/migrations:** squashed fresh `drizzle/0000_overrated_dakota_north.sql`. Added `audit_events` (id, event_type, object_type, object_reference, actor_role, detail, created_at) — see §6.
5. **Routes/services/APIs:** two new GET Route Handlers returning `application/pdf`: `/app/projects/:projectId/purchase-orders/:purchaseOrderId/pdf`, `/app/projects/:projectId/payment-vouchers/:voucherId/pdf`. No new Server Actions (read-only feature).
6. **Permissions/SoD:** document *generation* is gated by record status only (§7), not by actor role — the pack's SoD matrix doesn't name document-download as a role-restricted capability, unlike the mutating transitions gated in Checkpoint 7. What **is** new: `DOCUMENT_AND_REPORT_GENERATION_STANDARD.md`'s distribution rule ("every distribution ... writes an AuditEvent") was the direct trigger for finally adding a real (if minimal) `audit_events` table — a gap disclosed in every prior checkpoint's readiness sidecar since Checkpoint 1. Every PDF download writes one real row: event type, object type/reference, the current actor role, a timestamp. This is not the full immutable audit/evidence engine every readiness sidecar still discloses as missing (no tamper-evidence, no retention policy, no UI to browse it) — just a real, working slice triggered by an actual feature needing it, not fabricated ahead of need.
7. **Formula reconciliations:** none new — the PDFs render pre-computed figures from `getPurchaseOrderData`/`getPaymentVoucherData` (already reconciled in Checkpoints 5/6). Independently re-verified by reading the rendered PDF content: PO net 1,365,960 / statutory additions 273,192 / gross 1,639,152; PV 174,952 / 34,990 / 3,499 / 206,443 — both match the golden fixtures exactly.
8. **Evidence/lineage/audit:** `evidence/v7/checkpoint-8/audit-events-snapshot.txt` — a real database snapshot showing 11 rows accumulated from repeated live downloads during this checkpoint's own testing (not a single cherry-picked success). Each generated document embeds, per the standard's "Generation mechanics": generation timestamp, source record UUID, and an explicit "version 1 (no amendment/revision tracking yet)" statement rather than a fabricated version number — no document-versioning field exists on any table.
9. **Tests:** No automated test suite (same disclosed gap). Verification: `tsc --noEmit`, `eslint`, `next build` clean. Live-downloaded both PDFs via `curl` and via the browser button, confirmed valid `PDF document, version 1.7` output (not just a 200 status), rendered both in a real PDF viewer (Chromium's built-in renderer) and visually verified every figure. A regression check on Page 01 Control Room produced a byte-identical screenshot hash to Checkpoint 7's, confirming zero regression.
10. **Build and deterministic install:** `npm run build` succeeds. One new dependency: `pdf-lib` (pure JS, no native/browser dependency — deliberately not using a headless-browser-based renderer for this, since the app already has a real PDF-generation-capable library need and spinning up Chromium per request would be architecturally heavier than necessary).
11. **Browser states and viewports:** 1440x900 for page screenshots; PDF content captured as rendered by a real PDF viewer, not a mockup. `evidence/v7/checkpoint-8/`.
12. **Screenshot manifest and hashes:** `evidence/v7/checkpoint-8/screenshot-manifest.json`, including hashes of the two downloaded PDF files themselves, not just page screenshots.
13. **Region-by-region authority comparison:** not applicable — no approved visual template exists for print documents anywhere in `07_REFERENCE_ASSETS` (only web-page screenshots). This layout is disclosed as a first real generation pass, not matched against any approved PDF design authority, unlike the web pages built in Checkpoints 1–7.
14. **Console/network:** Zero console errors on the two page views. One real bug found and fixed *during* this checkpoint (not shipped): the PDF table-drawing helper let column widths sum past the page's usable width and didn't truncate long cell text, causing the line-item description to visually collide with the quantity column on both documents on first render. Fixed by adding text truncation-with-ellipsis to every table cell and a hard assertion that column widths must fit the page (`drawTable` now throws rather than silently overflow) — re-verified by re-rendering both PDFs and re-inspecting every figure.
15. **Accessibility/performance/security:** PDF documents are not scanned for PDF/UA accessibility (disclosed gap — out of scope for this checkpoint's bounded PDF-generation slice). Performance: not measured (small, synchronous, single-page documents; no pagination logic exists yet for a PO/PV with enough lines to overflow one page — disclosed gap). Security: `pdf-lib` was checked for known vulnerabilities at install time (none found); a pre-existing, unrelated `npm audit` finding was noted while touching `package.json` — `drizzle-kit`'s transitive `esbuild` dependency has a moderate dev-server-only advisory (GHSA-67mh-4wv8-2f99); the only fix is a breaking downgrade of `drizzle-kit` itself, which every checkpoint's migration workflow depends on, and the vulnerability only affects the local dev server, not the shipped app — deliberately not fixed this checkpoint, flagged here rather than silently ignored.
16. **Defects:**
    - *Major (disclosed, unresolved, inherited):* no real authentication (Checkpoint 7); no full evidence/audit engine (only the document-export slice added here); no forecast/cashflow model.
    - *Found and fixed this checkpoint:* the table-overflow/text-collision bug (§14) — caught by actually reading the rendered PDF, not just checking the HTTP status was 200.
    - *Minor:* PDF generation has no pagination — a PO/PV with enough lines to exceed one page would silently draw off the bottom of the page rather than flowing to a second page. Not reachable with current seed data (max 3 lines) but a real limitation for a production dataset.
17. **Approved variations:** none sought or granted.
18. **Owner decisions required before Checkpoint 9:** unchanged from `OWNER_ACCEPTANCE_PACK.md` §7, plus: (6) confirm "professional outputs" was an acceptable checkpoint to self-scope, or specify a different area from the remaining Phase-1 hardening basket (security/SAST, performance/load testing, migration, integration, backup/restore, observability) for future checkpoints; (7) the pre-existing `drizzle-kit`/`esbuild` dev-dependency advisory (§15) — accept as dev-only risk or invest in the breaking downgrade.
19. **Next proposed checkpoint (not started):** not specified — same open question as Checkpoint 7 (`OWNER_ACCEPTANCE_PACK.md` §7.5), now with one more concrete option resolved (professional outputs, done for 2 of ~14 named document types) and one more added (§18.6/§18.7). Not started; no code written toward it.
20. **Status:** `NOT OWNER-ACCEPTED`.

---

**Reproduction:** fresh seed → open `PO-2026-041` (P07) → click "Preview
Order PDF" (real link, opens the generated PDF in a new tab) → open
`PV-2026-0076` (P09) → click "Export PV PDF". Both work immediately since
both golden records are already `ISSUED`/`PAID`. Check `audit_events`
in the database to see the export logged.

CHECKPOINT 8 IMPLEMENTED AND VERIFIED — NOT OWNER-ACCEPTED — AWAITING
OWNER REVIEW — DO NOT START ANOTHER CHECKPOINT.
