# Owner Checkpoint Report

Using the template in `05_CLAUDE_CODE_EXECUTION/OWNER_CHECKPOINT_REPORT_TEMPLATE.md`.

1. **Checkpoint/page:** Checkpoint 11 — **self-scoped**, not pack-mandated (same situation as Checkpoints 8–10). Picks "security" from Checkpoint 7's deferred Phase-1 hardening basket, grounded in `SECURITY_AND_ACCESS_CONTROL_STANDARD.md`. That standard's full scope (email+password auth with mandatory MFA, SSO abstraction, 15-minute access tokens with rotating refresh tokens, per-user session lists, rate limiting, provider-managed at-rest encryption, application-level field encryption, a managed secrets vault, upload malware scanning, external annual penetration testing) requires real managed infrastructure — an IdP, a secrets manager, a WAF/rate-limiter, an object store, a pen-test vendor — none of which exist or can meaningfully exist in this sandbox. Rather than assert compliance with any of that, this checkpoint scopes down to the concretely checkable, buildable subset: secret-scanning, injection/XSS code audit, verifying authorization is never client-trusted, and a dependency vulnerability scan. Everything else in the standard remains an explicit, named gap — see §19.
2. **Branch and commit:** `claude/design-authority-review-azeq82`, on top of Checkpoints 1–10.
3. **Production files changed:** `src/server/actions/actor-actions.ts` (the actor-role cookie is now `httpOnly` — see §16), `scripts/security-audit.sh` (new), `.gitleaksignore` (new — 3 documented false-positive fingerprints).
4. **Schema/migrations:** none.
5. **Routes/services/APIs:** none. One new CLI script (`scripts/security-audit.sh`), intended as the CI secret-scanning + dependency-scanning job the standard names, run locally until a real CI pipeline exists.
6. **Permissions/SoD:** verified, not just asserted, this checkpoint: grepped every one of the 8 role-gated Server Actions' function signatures and confirmed none accepts a client-supplied `role`/`actorRole` parameter — every one calls `getActorRole()` internally, which reads the role from a server-side-only cookie store, never from client-passed arguments. The sole action that *does* take a `role` parameter is `setActorRole` itself, which is the disclosed self-service identity switcher (Checkpoint 7), not a permission-check bypass. This directly satisfies `SECURITY_AND_ACCESS_CONTROL_STANDARD.md`'s rule: "permission checks happen server-side on every request, never trusted from client state."
7. **Formula reconciliations:** none — no money logic touched.
8. **Evidence/lineage/audit:** `evidence/v7/checkpoint-11/security-audit-output.txt` — full real output of all four checks, not a summary. Continues Checkpoint 8's `audit_events`/export-logging work (already satisfies the standard's "every export... writes an AuditEvent" rule for the one export type this build has, PDF downloads).
9. **Tests:** No automated test suite (same disclosed gap), but `scripts/security-audit.sh` is itself a real, repeatable, exit-code-driven check — re-run it after any future dependency bump or code change and a regression (a new secret, a new raw-SQL string, a new `dangerouslySetInnerHTML`, a new high/critical CVE) fails loudly rather than silently.
10. **Build and deterministic install:** `npm run build`/`tsc --noEmit`/`eslint` clean. One new tool installed via `apt-get`: the real `gitleaks` binary (Ubuntu's `gitleaks` package, not an unrelated npm package of the same name) — not a project dependency, an audit tool.
11. **Browser states and viewports:** one screenshot (1440x900) exercising the role switcher live after the `httpOnly` cookie change, with Playwright's own cookie inspection logged alongside it confirming `httpOnly: true` at the real network layer, not just in the source code.
12. **Screenshot manifest and hashes:** `evidence/v7/checkpoint-11/screenshot-manifest.json`.
13. **Region-by-region authority comparison:** not applicable — no UI built this checkpoint.
14. **Console/network:** zero errors.
15. **Findings — the four checks run, and their results:**
    - **Secret scanning** (`gitleaks detect`, full 12-commit git history): 3 initial findings, all verified false positives — the `generic-api-key` entropy heuristic flagged real BOQ cost-code strings (e.g. `"MASON-GF-BLK150-098"`) in `src/db/seed.ts` as if they were credentials. Verified by reading the surrounding code (`authorityReference:` fields, sourced from the workbook, not secrets) and allowlisted by exact commit+file+line fingerprint in `.gitleaksignore`, not by disabling the rule. Also independently confirmed `.env` (holding the local `postgres:postgres` dev credential) has never been committed, in any commit, ever (`git log --all -- .env` returns nothing) and is correctly `.gitignore`d.
    - **SQL injection audit**: zero raw `` sql`...` `` template usage anywhere in `src/`. Every application database access goes through Drizzle's parameterized query builder. One reviewed exception in operator tooling: `scripts/reconcile.ts` (Checkpoint 9) interpolates a table name into a raw `pg.Client.query()` call — safe because the interpolated value only ever comes from a fixed, hardcoded `TABLES` array in the same file, never from user/network input (table names can't be `$1`-parameterized in Postgres in any driver; allowlisting the identifier is the correct pattern here), and the script itself is operator-only tooling, not an application code path reachable from the web app.
    - **XSS audit**: zero `dangerouslySetInnerHTML`, `eval`, or `new Function` usage anywhere in `src/`. Every dynamic value rendered in JSX goes through React's default auto-escaping.
    - **Dependency vulnerability scan**: `npm audit --omit=dev` — zero vulnerabilities in production dependencies. `npm audit` (including dev dependencies) still shows the one moderate `esbuild`/`drizzle-kit` advisory first disclosed in Checkpoint 8 (dev-server-only, unrelated to this checkpoint, unchanged since — the breaking downgrade required to clear it remains an open owner decision, `OWNER_ACCEPTANCE_PACK.md` §7.7).
16. **Defects:**
    - *Found and fixed this checkpoint:* the actor-role cookie was not `httpOnly`, with no functional reason for it not to be (nothing in the app reads `document.cookie` — verified by grep — the `RoleSwitcher` component receives its value as a server-rendered prop, and writes go through the `setActorRole` Server Action, never direct client-side cookie writes). Made `httpOnly: true`; re-verified live that the switcher still works identically (§11).
    - *Found, disclosed, not a defect:* the 3 gitleaks false positives (§15) and the reconcile.ts table-name interpolation (§15) — both reviewed and confirmed safe, not silently ignored.
17. **Approved variations:** none sought or granted.
18. **Owner decisions required before Checkpoint 12:** unchanged from `OWNER_ACCEPTANCE_PACK.md` §7 (still nine open items) — this checkpoint didn't add a new one, since everything found was either fixed or verified safe.
19. **Next proposed checkpoint (not started):** not specified — same open question as Checkpoints 7–10. What `SECURITY_AND_ACCESS_CONTROL_STANDARD.md` still names that this checkpoint could not touch: real authentication (email+password+MFA — overlaps `OWNER_ACCEPTANCE_PACK.md` §7.5), session/token management, rate limiting, encryption-at-rest/in-transit beyond local defaults, a secrets vault, upload/malware scanning (no uploads exist in this build yet), external penetration testing (requires a real deployed target and a vendor, not something this sandbox can self-provide). Remaining untouched items from the broader Phase-1 hardening basket: observability (structured logging, health-check endpoint), migration (incremental upgrade path never tested), integration. Not started; no code written toward any of them.
20. **Status:** `NOT OWNER-ACCEPTED`.

---

**Reproduction:** `bash scripts/security-audit.sh` — runs all four checks
in sequence, exits non-zero on the first failure.

CHECKPOINT 11 IMPLEMENTED AND VERIFIED — NOT OWNER-ACCEPTED — AWAITING
OWNER REVIEW — DO NOT START ANOTHER CHECKPOINT.
