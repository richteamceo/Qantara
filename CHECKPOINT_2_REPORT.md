# Owner Checkpoint Report

Using the template in `05_CLAUDE_CODE_EXECUTION/OWNER_CHECKPOINT_REPORT_TEMPLATE.md`.

1. **Checkpoint/page:** Checkpoint 2 — Requests Register (P02) + Request Dossier (P03).
2. **Branch and commit:** `claude/design-authority-review-azeq82`, on top of the Checkpoint 1 + addendum commits.
3. **Production files changed:** `src/server/requests-register.ts`, `src/server/request-dossier.ts`, `src/server/actions/request-actions.ts` (new), `src/app/app/projects/[projectId]/requests/**` (new: register + dossier routes), `src/components/requests/*` (new), `src/components/shell/DomainPanel.tsx` (refactored to real route-based active state, "Requests" link enabled), `src/components/shell/AppShell.tsx` / project `layout.tsx` (dropped the now-redundant `activeHref` prop), `src/db/schema.ts` / `src/db/seed.ts` (new `request_lines` table, second control account, demo-data flag — see §4), `src/server/control-room.ts` + `LifecycleSpine.tsx` / `ControlSheet.tsx` (currency-guard extended to cover the pipeline column — see §7).
4. **Schema/migrations:** squashed fresh `drizzle/0000_glamorous_saracen.sql` (dev-only data, no real users to migrate forward). Added `request_lines` (13 columns), a second control account ("Block & Masonry (BOQ trade rollup)", real BOQ-sourced like Checkpoint 1's addendum), `requests.currency`/`requestedBy`/`workArea`/`needByDate`/`priority`/`isDemoData`/`demoNote`; removed `requests.quantity`/`unit` (superseded by per-line quantity/unit, which is more correct for a multi-line request).
5. **Routes/services/APIs:** `/app/projects/:projectId/requests` and `/app/projects/:projectId/requests/:requestId` — both match the pack's canonical route shapes exactly. New server aggregation: `getRequestsRegisterData`, `getRequestDossierData` (the latter reuses `getControlRoomData`'s per-account variance rather than re-deriving currency-guard logic). One real mutation: the `approvePrepareRequestPackage` Server Action (Next.js `"use server"`), not a REST/JSON API — no HTTP API layer exists yet, consistent with Checkpoint 1's disclosed gap.
6. **Permissions/SoD:** Still not implemented — same disclosed gap as Checkpoint 1 (§6 there). The action's own step 1 ("revalidate permission") is a no-op; only state (existing package / authority / status) is checked.
7. **Formula reconciliations:** see `evidence/v7/checkpoint-2/formula-reconciliation.md`. Highlights: (a) every request's line total is checked against its own `controlledEstimate` and both currently reconcile; (b) independently re-verified all 4 request-line BOQ/exception references against the real workbook — 3 of 4 found, 1 not found (the golden fixture's own pump line, `PLANT-SUB-PUMP-031`), and that contradiction is surfaced rather than smoothed over (the already-`APPROVED` golden request scores 40% readiness because of it); (c) extended the Checkpoint-1-addendum currency guard to the lifecycle spine and control-sheet pipeline column, and it now demonstrably computes a real, correct, currency-consistent variance for the new Block & Masonry account (not just the "incomplete" branch as before).
8. **Evidence/lineage/audit:** No audit-event log or evidence-hash chain still (unchanged gap). The Approve & Prepare Package action does record *what* it did via its own typed return value and `revalidatePath` calls, but nothing is persisted as an immutable audit row yet.
9. **Tests:** No automated test suite. Verification this checkpoint: `tsc --noEmit`, `eslint`, `next build` all clean; Playwright pass across both new pages plus the golden and demo dossiers, zero console errors on every page; the Approve & Prepare Package transition was exercised live end-to-end (not just unit-tested) with before/after screenshots; the transition's *blocked* branch was separately verified via a standalone throwaway-data script (inserted a temp request with a deliberately-invalid BOQ reference, confirmed the action correctly refused and created nothing, deleted the temp rows) since neither seeded request can currently reach that branch through the UI.
10. **Build and deterministic install:** `npm run build` succeeds. `package-lock.json` unchanged (no new dependencies this checkpoint).
11. **Browser states and viewports:** "Populated" state only, at 1440×900, for both pages, plus the live before/after transition state on the dossier. The bulk of each page's contract-required state list (loading, error, empty, restricted, etc. — see `evidence/v7/checkpoint-2/screenshot-manifest.json` for the full itemized list) is not covered. 1280×800/390×844 responsive viewports not captured.
12. **Screenshot manifest and hashes:** `evidence/v7/checkpoint-2/screenshot-manifest.json`.
13. **Region-by-region authority comparison** against `07_REFERENCE_ASSETS/APPROVED_SCREENSHOTS/02-requests-register.jpg` / `03-request-dossier.jpg`: **not performed** — same disclosed scope cut as Checkpoint 1 (Gate 3/5 work). What was built for P02: header/actions (Export and New Request both disabled/disclosed), 5-cell KPI strip (all real, 2 of 5 currency-aware, 1 explicitly incomplete), 5 of 8 saved views (All/My Open/Needs Action/BOQ Exceptions/Awaiting Procurement — 3 omitted and named as such), all 13 required grid columns, row-click to dossier. For P03: header (state/priority/cost-type/line-count/need-by/owner shown; SLA and golden-transaction rail explicitly deferred), 4-cell KPI strip, 3 of 6 tabs fully real (Overview, Lines & Allocations, Commercial Controls — partial), readiness sidecar, the live primary transition. Not attempted: Documents/Workflow/Audit Trail tab content, Full Lineage drawer, Return for Correction, duplicate-request detection, approval threshold/SoD checks, evidence completeness.
14. **Console/network:** Zero console errors/warnings and zero failed requests observed across all pages and the live transition (register, both dossiers, before/after screenshots, control-room re-check).
15. **Accessibility/performance/security:** Same disclosed gap as Checkpoint 1 — no automated a11y/perf/security scan run. Basic manual choices carried forward (focus-visible outline, `aria-current`, `aria-disabled` + `title` on non-functional controls); the register's rows are not yet keyboard/Enter-openable (only the reference link is, via `<a>`) — a real gap against PAGE_02's row-behaviour contract ("whole row opens Request Dossier ... Enter/Space equivalent"), not yet fixed.
16. **Defects:**
    - *Major (disclosed, unresolved, inherited):* no auth/RBAC (§6); no evidence/audit engine (§8).
    - *Major (disclosed, unresolved, new):* Register rows open via the reference link only, not the whole row / keyboard Enter, contrary to PAGE_02's row-behaviour contract.
    - *Major (disclosed, unresolved, new):* `requests.quantity`/`unit` columns from Checkpoint 1 were dropped in favor of per-line quantity/unit — correct for the data model, but means any earlier reference to a single request-level quantity is gone; not an issue for the current 2 seeded requests, worth knowing before building more on top.
    - *Minor:* Median cycle KPI explicitly incomplete (no workflow-transition timestamps modelled — only `createdAt`); 3 of 8 P02 saved views and 3 of 6 P03 tabs not implemented, disclosed inline in the UI ("soon" badges) and here.
    - *Critical fixed before evidence capture:* none this checkpoint (Checkpoint 1's spine-double-counting class of bug was checked for and not reproduced — see reconciliation doc).
17. **Approved variations:** none sought or granted.
18. **Owner decisions required before Checkpoint 3:**
    - The golden fixture's own contradiction (APPROVED request with one line's BOQ authority not found in the source workbook, MR-SWTBK-2026-0035 line 2) — should this be corrected in the fixture, treated as a known historical exception to backfill an `EXC-` code for, or left as a deliberate "the system catches problems even in already-approved data" demonstration?
    - Register row-opening behavior (whole-row click/Enter) should be fixed — flagging now rather than letting it compound into Checkpoint 3's Procurement Package register.
    - Same three open items carried from `CHECKPOINT_1_ADDENDUM.md` (mixed-currency transaction modeling across the whole chain, control-account grain, FX rate source) remain open and are now slightly more pressing since a second, genuinely-different-currency request exists.
19. **Next proposed checkpoint (not started):** Checkpoint 3 — Pages 04 and 05 (Procurement Package/Bid Comparison, Award Decision), per the pack's page-implementation order. Not started; no code written toward it.
20. **Status:** `NOT OWNER-ACCEPTED`.

---

**Reproduction:** same as Checkpoint 1 (`npm install`, Postgres + `.env`,
`npx drizzle-kit migrate`, `npx tsx src/db/seed.ts`, `npm run dev`), then
visit `/app/projects/SWTBK/requests`. Seeding is idempotent from empty —
re-running against a fresh database reproduces exactly the "before"
screenshots; the "after" screenshots reflect one live Approve & Prepare
Package click on MR-DEMO-0001 on top of that, which you can reproduce by
clicking the button on that request's dossier.

CHECKPOINT 2 IMPLEMENTED AND VERIFIED — NOT OWNER-ACCEPTED — AWAITING
OWNER REVIEW — DO NOT START ANOTHER PAGE.
